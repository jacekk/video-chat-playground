import { chatClients } from './chat/clients'
import { chatMessages } from './chat/messages'
import { serverConnection } from './serverConnection'

export const model = {
	chatClients,
	chatMessages,
	serverConnection,
}
